\documentclass[12pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{textcomp}
\usepackage{enumitem}
\usepackage{geometry}
\geometry{margin=1in}
\begin{document}
\begin{center}
Answer Key - CSE-476: Introduction to Natural Language Processing \\
Quiz 2 \\
\small (Answers are ordered exactly as in the question paper.)
\end{center}

\section*{Multiple Choice Questions}
\begin{enumerate}[label=Q\arabic*.]
\item \textbf{Answer:} A
\\ \textit{Options:} A) Generative models model $P(x,y)$, while discriminative models model $P(y \mid x)$; B) Discriminative models learn the data distribution and use that knowledge for classification; C) Discriminative models require no training data; D) Generative models are less explainable
\\ \textit{Note:} Correct option: A - Generative classifiers model the joint distribution $P(x,y)$; discriminative classifiers model the conditional $P(y \mid x)$.
\item \textbf{Answer:} B
\\ \textit{Options:} A) Laplace smoothing; B) Regularization; C) TF-IDF; D) Softmax normalization
\\ \textit{Note:} Correct option: B - Regularization penalizes large weights (e.g., L1/L2) to reduce overfitting.
\item \textbf{Answer:} B
\\ \textit{Options:} A) It cannot use vector representations; B) It only works for linearly separable data; C) It requires probabilistic outputs; D) It cannot be trained with gradient descent
\\ \textit{Note:} Correct option: B - The perceptron finds a linear decision boundary; it cannot classify non-linearly separable data.
\item \textbf{Answer:} C
\\ \textit{Options:} A) It improves cosine similarity; B) It reduces dimensionality; C) It avoids zero probabilities for unseen words; D) It speeds up gradient descent
\\ \textit{Note:} Correct option: C - Laplace smoothing adds a small count so that unseen words (e.g., in test data) get non-zero probability instead of zero.
\item \textbf{Answer:} B
\\ \textit{Options:} A) SGD updates weights using the entire dataset every step; B) SGD updates weights using one or a subset of training examples; C) GD converges faster because updates are noisier; D) SGD cannot be used with neural networks
\\ \textit{Note:} Correct option: B - SGD uses one or a mini-batch of examples per update; full-batch GD uses the entire dataset every step.
\item \textbf{Answer:} B
\\ \textit{Options:} A) Words are represented by their dictionary definitions; B) Words that appear in similar contexts have similar meanings; C) Words are clustered by part of speech; D) Words are ranked by frequency
\\ \textit{Note:} Correct option: B - The distributional hypothesis: similar context implies similar meaning; embeddings capture this.
\item \textbf{Answer:} D
\\ \textit{Options:} A) To penalize large model weights; B) To reduce feature dimensionality; C) To select the class with the largest raw score without normalization; D) To convert raw class scores into a valid probability distribution
\\ \textit{Note:} Correct option: D - Softmax converts logits (raw scores) into probabilities that sum to 1.
\item \textbf{Answer:} C
\\ \textit{Options:} A) After every training example; B) Only when the prediction is correct; C) Only when the prediction is incorrect; D) Only after one full pass over the data
\\ \textit{Note:} Correct option: C - The perceptron updates weights only on misclassification (when the prediction is incorrect).
\item \textbf{Answer:} D
\\ \textit{Options:} A) Increase the weight of very common words; B) Represent word order; C) Convert sparse vectors into dense embeddings; D) Highlight words that are frequent in a document but rare across documents
\\ \textit{Note:} Correct option: D - TF-IDF upweights terms that are frequent in the document (TF) but rare in the corpus (IDF), capturing discriminative content.
\item \textbf{Answer:} A
\\ \textit{Options:} A) It ignores vector length while calculating similarity; B) It increases the influence of high-frequency words; C) It converts similarity scores into probabilities; D) It reduces the dimensionality of word embeddings
\\ \textit{Note:} Correct option: A - Cosine similarity is based on angle (direction), not magnitude, so vector length does not affect the similarity score.
\end{enumerate}

\vfill
\noindent\textit{End of Answer Key}
\end{document}
