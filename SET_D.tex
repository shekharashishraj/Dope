 \documentclass[12pt]{article}
 \usepackage[T1]{fontenc}
 \usepackage[utf8]{inputenc}
 \usepackage{lmodern}
 \usepackage{textcomp}
 \usepackage{enumitem}
 \usepackage{geometry}
 \usepackage{graphicx}
 \usepackage{amsmath, amssymb}
 \geometry{margin=1in, top=0.5in}
 
 \begin{document}
 
 \begin{center}
 \includegraphics[width=2.5cm]{asu-logo.png} \\[0.2cm]
 CSE476 -- Spring 2026 \\
 Mid-Term I -- SET D \\
 \end{center}
 
 \vspace{0.5cm}
 
 \noindent Name:\ \rule{6cm}{0.4pt} \hfill ASU ID:\ \rule{6cm}{0.4pt}
 
 \vspace{0.5cm}
 
 \section*{Instructions}
 \begin{itemize}
     \item Fill your Name and ASU ID above.
     \item There are 34 questions, the first question worth 1 point, and the remaining 33 questions worth 3 points each, totaling 100 points. This exam is graded out of 100 points.
     \item Select the one best answer for each question.
     \item You may use two cheat sheets, handwritten or typed, double-sided, and a calculator on this exam.
     \item Do not use AI, web or any other documents to answer the questions.
     \item By taking this exam, you agree to abide by the Academic Integrity policy established within the syllabus and any additional rules discussed in the class.
 \end{itemize}
 
 \vspace{0.5cm}
 
 \section*{Single Answer Multiple Choice Questions}
 \textit{Select the one best answer for each question.}
 
 \begin{enumerate}[label=Q\arabic*.]
 
 \item Which set is this?
 \begin{enumerate}[label=(\Alph*)]
     \item A
     \item B
     \item C
     \item D
 \end{enumerate}
 
 \item Suppose two standard non-zero TF-IDF vectors have cosine similarity $-0.2$. What does this imply?
 \begin{enumerate}[label=(\Alph*)]
     \item The documents are strongly unrelated in topic
     \item They have negatively correlated term weights
     \item The similarity computation was implemented incorrectly
     \item The documents share no terms
 \end{enumerate}
 
 \item Why do deeper networks require careful initialization?
 \begin{enumerate}[label=(\Alph*)]
     \item To control gradient scale across layers
     \item To decrease the vocabulary size of inputs
     \item To simplify the objective function
     \item To remove recurrence from the model
 \end{enumerate}
 
 \item Stacking multiple linear layers without activations results in:
 \begin{enumerate}[label=(\Alph*)]
     \item Implicit regularization of weights
     \item Exponentially more expressive representations
     \item Increased ability to model hierarchical structure
     \item A model equivalent to a single linear transformation
 \end{enumerate}
 
 \item Why do vanilla RNNs struggle with long-range dependencies?
 \begin{enumerate}[label=(\Alph*)]
     \item The hidden state has fixed dimensionality
     \item They reuse identical parameters at every time step
     \item Gradients decay or explode during backpropagation
     \item They produce outputs only at the final step
 \end{enumerate}
 
 \item Why does the Markov assumption reduce sparsity in N-gram models?
 \begin{enumerate}[label=(\Alph*)]
     \item It removes independence assumptions
     \item It restricts conditioning context
     \item It increases vocabulary size
     \item It enforces smoothing
 \end{enumerate}
 
 \item A two-layer neural network with step activations can represent the XOR function. However, when trained with gradient descent, it fails to learn XOR. What best explains this discrepancy?
 \begin{enumerate}[label=(\Alph*)]
     \item Step activations are insufficient to represent nonlinear decision boundaries
     \item The loss function is not convex for XOR
     \item The network requires more hidden units to separate XOR
     \item The activation provides no useful local gradient signal for weight updates
 \end{enumerate}
 
 \item A neural language model is trained on the phrases ``small red car'' and ``large blue truck.'' During testing, it assigns meaningful probability mass to ``small blue car,'' which never appeared in training. What most directly enables this generalization?
 \begin{enumerate}[label=(\Alph*)]
     \item The model stores all observed word pairs explicitly
     \item Embeddings capture semantics that combine compositionally
     \item The softmax layer guarantees coverage of unseen sequences
     \item Training implicitly includes all possible word permutations
 \end{enumerate}
 
 \item A TF-IDF retrieval system often misses semantically equivalent documents with synonyms. The primary limitation is:
 \begin{enumerate}[label=(\Alph*)]
     \item Bag-of-Words representation assumes term independence
     \item IDF overweights rare tokens
     \item Document length normalization distorts similarity
     \item Cosine similarity ignores magnitude differences
 \end{enumerate}
 
 \item Increasing model size worsens test performance while improving training accuracy. This suggests:
 \begin{enumerate}[label=(\Alph*)]
     \item Optimization failure
     \item Underfitting
     \item Overfitting
     \item Over-regularization
 \end{enumerate}
 
 \item When computing the loss at time $t$ in an RNN, what differs from a feed-forward network?
 \begin{enumerate}[label=(\Alph*)]
     \item The RNN uses different weights at each step
     \item The RNN ignores its previous hidden state
     \item The RNN does not produce outputs at intermediate steps
     \item The loss depends on the hidden state from time $t-1$ and the same weight matrices are reused across all time steps
 \end{enumerate}
 
 \item L2 regularization improves generalization primarily by:
 \begin{enumerate}[label=(\Alph*)]
     \item Forcing sparse parameter updates
     \item Encouraging smaller weight magnitudes
     \item Reducing the number of layers in the network
     \item Eliminating bias terms
 \end{enumerate}
 
 \item A sigmoid neuron slows learning when its activation saturates because:
 \begin{enumerate}[label=(\Alph*)]
     \item Bias parameters stop updating
     \item Gradients become very small in extreme regions
     \item Weight magnitudes increase uncontrollably
     \item Its output becomes approximately linear
 \end{enumerate}
 
 \item RNN models share parameters across time steps. Compared to a model with independent parameters at each step, weight reuse primarily:
 \begin{enumerate}[label=(\Alph*)]
     \item Allows parallel computation across sequence positions
     \item Eliminates vanishing gradients through parameter averaging
     \item Increases model capacity by expanding representational depth
     \item Reduces parameters and shares the same transformation
 \end{enumerate}
 
 \item In RNN language modeling, the typical training loss is:
 \begin{enumerate}[label=(\Alph*)]
     \item Cross-entropy over the vocabulary
     \item Smooth L1 over embeddings
     \item Hinge loss over tags
     \item Binary cross-entropy per position
 \end{enumerate}
 
 \item Suppose a document is tokenized at the sentence level (each entire sentence is one token). What impact does this have on vocabulary size and sequence length?
 \begin{enumerate}[label=(\Alph*)]
     \item Both vocabulary size and sequence length decrease
     \item Vocabulary size increases dramatically, and sequence length becomes very short
     \item Both vocabulary size and sequence length increase
     \item Vocabulary size decreases dramatically, and sequence length becomes extremely long
 \end{enumerate}
 
 \item Why must an RNN be unrolled across time during training?
 \begin{enumerate}[label=(\Alph*)]
     \item To allocate separate parameters for each time step
     \item To represent recurrence as a time-indexed differentiable graph
     \item To remove weight sharing across sequence positions
     \item To increase hidden state dimensional capacity
 \end{enumerate}
 
 \item LSTMs mitigate basic RNN issues mainly by:
 \begin{enumerate}[label=(\Alph*)]
     \item Freezing the recurrent matrix $U$
     \item Introducing gated operations
     \item Using convolution instead of recurrence
     \item Removing nonlinearity between layers
 \end{enumerate}
 
 \item Which NLP architecture would be suitable for identifying the NER type for ``Ben'' in ``Ben \& Jerry's ice cream is good''?
 \begin{enumerate}[label=(\Alph*)]
     \item BiLSTM
     \item Word2Vec + Decoder RNN
     \item Bag-of-Words + Naive Bayes
     \item Hidden Markov Models
 \end{enumerate}
 
 \item A 3-layer neural network with ReLU activations outputs only zeros for all inputs after initialization. What is the most likely cause?
 \begin{enumerate}[label=(\Alph*)]
     \item Cross-entropy loss misconfigured
     \item The final layer is using Softmax without temperature scaling
     \item Learning rate too high
     \item All pre-activation values are negative in the first layer
 \end{enumerate}
 
 \item A 3-layer neural network with ReLU activations outputs a fixed, non-zero output for all inputs. What is the most likely cause?
 \begin{enumerate}[label=(\Alph*)]
     \item Dropout disabled
     \item The input data has not been normalized
     \item All weights are initialized to zero while biases are non-zero
     \item Learning rate too high
 \end{enumerate}
 
 \item Compared to full-batch gradient descent, SGD introduces:
 \begin{enumerate}[label=(\Alph*)]
     \item Biased gradient estimates but lower variance
     \item Deterministic updates with faster convergence
     \item Unbiased gradient estimates with higher variance
     \item Smaller parameter magnitudes
 \end{enumerate}
 
 \item Why does BPE reduce out-of-vocabulary issues?
 \begin{enumerate}[label=(\Alph*)]
     \item It removes infrequent words from the vocabulary
     \item It increases vocabulary size
     \item It decomposes rare words into subword units
     \item It converts tokens into fixed-length character blocks
 \end{enumerate}
 
 \item A sequence model trained with teacher forcing performs well during training but poorly during inference. The most likely explanation is:
 \begin{enumerate}[label=(\Alph*)]
     \item Cross-entropy loss over-penalizes rare tokens during decoding
     \item The model experiences exposure bias due to distribution mismatch
     \item The hidden state dimensionality is insufficient for long sequences
     \item The optimizer fails to converge to a global minimum
 \end{enumerate}
 
 \item In standard LSTMs, which component carries long-term summary information?
 \begin{enumerate}[label=(\Alph*)]
     \item Input embeddings
     \item Output logits
     \item Cell state $c$
     \item Hidden state $h$
 \end{enumerate}
 
 \item Two non-zero TF--IDF document vectors have cosine similarity $0$. What does this imply?
 \begin{enumerate}[label=(\Alph*)]
     \item Their Euclidean distance must be maximal
     \item The documents have different lengths
     \item The documents are unrelated in meaning
     \item The documents share no common terms
 \end{enumerate}
 
 \item A tokenizer converts a sentence into a sequence like: \([12408, 6391, 4014, 316, 1001]\). What best explains why tokens are converted into this form?
 \begin{enumerate}[label=(\Alph*)]
     \item To enable the model to process text as numerical input based on a fixed vocabulary mapping
     \item To ensure every word has a single unchanging meaning
     \item To compress the sentence into a shorter representation
     \item To remove grammatical ambiguity before training
 \end{enumerate}
 
 \item What is the probability of the sequence $H \rightarrow C \rightarrow H$? Given:
 \[
 P(H \rightarrow H) = 0.7,\quad
 P(H \rightarrow C) = 0.3,\quad
 P(C \rightarrow H) = 0.4,\quad
 P(C \rightarrow C) = 0.6,
 \]
 with initial probabilities \(P(H) = 0.5, P(C) = 0.5\).
 \begin{enumerate}[label=(\Alph*)]
     \item $0.12$
     \item $0.06$
     \item $0.21$
     \item $0.042$
 \end{enumerate}
 
 \item A language model achieves lower perplexity than another model but performs worse on question answering. The most principled explanation is:
 \begin{enumerate}[label=(\Alph*)]
     \item Lower perplexity always indicates overfitting to training data
     \item Perplexity ignores normalization across token positions
     \item Question answering requires larger vocabularies than modeling
     \item Perplexity reflects intrinsic likelihood rather than task utility
 \end{enumerate}
 
 \item During training, a neural network's loss decreases for a few iterations, then begins oscillating widely without converging. Which explanation is most consistent with this behavior?
 \begin{enumerate}[label=(\Alph*)]
     \item The batch size is too small, increasing gradient variability
     \item The batch size is too large, reducing gradient noise
     \item The learning rate is too small to escape local minima
     \item The learning rate is too large for the local loss curvature
 \end{enumerate}
 
 \item Consider a 4-word sequence $W = (w_1, w_2, w_3, w_4)$ with probabilities
 \[
 P(w_1) = 0.5,\quad
 P(w_2 \mid w_1) = 0.4,\quad
 P(w_3 \mid w_1, w_2) = 0.2,\quad
 P(w_4 \mid w_1, w_2, w_3) = 0.1.
 \]
 What is the perplexity of the sequence?
 \begin{enumerate}[label=(\Alph*)]
     \item $4.215$
     \item $2.154$
     \item $5.000$
     \item $3.976$
 \end{enumerate}
 
 \item A system has two states, $A$ and $B$, with the following transition probabilities:
 \begin{itemize}
     \item If in $A$: stays in $A$ with probability $0.6$ (and goes to $B$ with probability $0.4$)
     \item If in $B$: stays in $B$ with probability $0.4$ (and goes to $A$ with probability $0.6$)
 \end{itemize}
 Suppose the system starts in state $B$. As the number of transitions approaches infinity, what is the most accurate statement?
 \begin{enumerate}[label=(\Alph*)]
     \item The state probabilities converge to a fixed stationary distribution
     \item The state probabilities continue fluctuating without convergence
     \item The limiting distribution depends on the initial state
     \item The chain eventually remains permanently in one state
 \end{enumerate}
 
 \item In the Word2Vec skip-gram model with negative sampling, which of the following best describes the optimization objective for a target word $w$ and a context word $c$?
 \begin{enumerate}[label=(\Alph*)]
     \item Minimize the squared Euclidean distance between embeddings of all words in the same document
     \item Maximize $\log \sigma(v_c^\top v_w) + \sum_{i=1}^{k} \log \sigma(-v_{n_i}^\top v_w)$
     \item Maximize cosine similarity between all word pairs within a fixed window
     \item Maximize $\log P(c \mid w)$ using a full softmax over the vocabulary
 \end{enumerate}
 
 \item What fundamentally distinguishes ELMo embeddings from Word2Vec?
 \begin{enumerate}[label=(\Alph*)]
     \item It represents words using character-level frequency features
     \item It learns embeddings solely from global co-occurrence statistics
     \item It produces context-dependent representations of words
     \item It assigns a single fixed vector to each word type
 \end{enumerate}
 
 \end{enumerate}
 
 \vfill
 \noindent\textit{End of Paper}
 
 \end{document}
 
