"""Prevention-mode injection methods.

These are parallel implementations to the detection-focused injectors under `src/injection`.
They are designed for high-coverage text corruption (stems + options) while keeping
the existing detection pipeline untouched.
"""

