 \documentclass[12pt]{article}
 \usepackage[T1]{fontenc}
 \usepackage[utf8]{inputenc}
 \usepackage{lmodern}
 \usepackage{textcomp}
 \usepackage{enumitem}
 \usepackage{geometry}
 \geometry{margin=1in}
 
 \begin{document}
 
 \begin{center}
 Answer Key -- CSE476: Introduction to Natural Language Processing \\
 Mid-Term I -- SET D \\
 \small (Answers are ordered exactly as in the question paper.)
 \end{center}
 
 \section*{Single Answer Multiple Choice Questions}
 
 \begin{enumerate}[label=Q\arabic*.]
 
 \item \textbf{Answer:} D \\
 \textit{Note:} This exam is labeled as Set D.
 
\item \textbf{Answer:} C \\
\textit{Note:} A negative cosine similarity for standard non-zero TF--IDF vectors suggests an implementation issue, since cosine similarity for such vectors should be in $[0,1]$.
 
 \item \textbf{Answer:} A \\
 \textit{Note:} Proper initialization keeps gradients at a reasonable scale across layers.
 
 \item \textbf{Answer:} D \\
 \textit{Note:} A composition of linear layers without nonlinearities is equivalent to a single linear transformation.
 
 \item \textbf{Answer:} C \\
 \textit{Note:} Vanilla RNNs suffer from vanishing and exploding gradients over long time spans.
 
 \item \textbf{Answer:} B \\
 \textit{Note:} The Markov assumption restricts the conditioning context, reducing the number of distinct $n$-gram contexts.
 
 \item \textbf{Answer:} D \\
 \textit{Note:} Step activations provide no useful local gradient signal for gradient descent almost everywhere.
 
 \item \textbf{Answer:} B \\
 \textit{Note:} Distributed word embeddings allow compositional generalization to unseen but related phrases.
 
 \item \textbf{Answer:} A \\
 \textit{Note:} A bag-of-words representation with TF--IDF relies on exact lexical overlap and ignores synonymy.
 
 \item \textbf{Answer:} C \\
 \textit{Note:} Higher capacity improves training fit but can hurt test performance due to overfitting.
 
 \item \textbf{Answer:} D \\
 \textit{Note:} In RNNs, the same weights are reused at each time step and the loss at time $t$ depends on the hidden state from $t-1$.
 
 \item \textbf{Answer:} B \\
 \textit{Note:} L2 regularization penalizes large weights, encouraging smaller magnitudes that improve generalization.
 
 \item \textbf{Answer:} B \\
 \textit{Note:} When a sigmoid saturates, its derivative is near zero, leading to very small gradients.
 
 \item \textbf{Answer:} D \\
 \textit{Note:} Sharing parameters across time reduces the number of parameters and applies the same transformation at each step.
 
 \item \textbf{Answer:} A \\
 \textit{Note:} RNN language models typically use cross-entropy loss over the vocabulary.
 
 \item \textbf{Answer:} B \\
 \textit{Note:} Treating whole sentences as tokens yields very short sequences but an extremely large vocabulary.
 
 \item \textbf{Answer:} B \\
 \textit{Note:} Unrolling builds a time-indexed computation graph so gradients can flow through time during backpropagation.
 
 \item \textbf{Answer:} B \\
 \textit{Note:} LSTMs use gated mechanisms to control information flow and alleviate basic RNN gradient issues.
 
 \item \textbf{Answer:} A \\
 \textit{Note:} A BiLSTM is a standard architecture for sequence labeling tasks like NER.
 
 \item \textbf{Answer:} D \\
 \textit{Note:} If all first-layer pre-activations are negative, ReLU units output zero and the network produces all zeros.
 
 \item \textbf{Answer:} C \\
 \textit{Note:} Zero weights with non-zero biases give the same pre-activation and thus the same output for every input.
 
 \item \textbf{Answer:} C \\
 \textit{Note:} SGD uses unbiased but noisy gradient estimates, resulting in higher variance than full-batch updates.
 
 \item \textbf{Answer:} C \\
 \textit{Note:} BPE breaks rare words into subword units, allowing representation of many previously OOV forms.
 
 \item \textbf{Answer:} B \\
 \textit{Note:} With teacher forcing, the model is trained on gold histories but at test time must condition on its own predictions, causing exposure bias.
 
 \item \textbf{Answer:} C \\
 \textit{Note:} In LSTMs, the cell state $c$ is designed to carry long-term summary information.
 
 \item \textbf{Answer:} D \\
 \textit{Note:} With non-negative TF--IDF vectors, cosine similarity $0$ implies no shared non-zero features (no shared terms).
 
 \item \textbf{Answer:} A \\
 \textit{Note:} Token IDs allow the model to consume text as numerical input via a fixed vocabulary mapping.
 
 \item \textbf{Answer:} B \\
 \textit{Note:} $P(H) \cdot P(H \rightarrow C) \cdot P(C \rightarrow H) = 0.5 \times 0.3 \times 0.4 = 0.06$.
 
 \item \textbf{Answer:} D \\
 \textit{Note:} Perplexity measures how well a model fits sequence likelihood, which need not correlate perfectly with downstream task performance.
 
 \item \textbf{Answer:} D \\
 \textit{Note:} A learning rate that is too large causes updates to overshoot minima, leading to oscillating loss.
 
 \item \textbf{Answer:} D \\
 \textit{Note:} The sequence probability is $0.5 \times 0.4 \times 0.2 \times 0.1 = 0.004$ and perplexity $= 0.004^{-1/4} \approx 3.976$.
 
 \item \textbf{Answer:} A \\
 \textit{Note:} This finite Markov chain is irreducible and aperiodic, so state probabilities converge to a unique stationary distribution independent of the start state.
 
 \item \textbf{Answer:} B \\
 \textit{Note:} Skip-gram with negative sampling maximizes $\log \sigma(v_c^\top v_w) + \sum_{i=1}^{k} \log \sigma(-v_{n_i}^\top v_w)$.
 
 \item \textbf{Answer:} C \\
 \textit{Note:} ELMo produces context-dependent word representations, whereas Word2Vec assigns a single vector per word type.
 
 \end{enumerate}
 
 \vfill
 \noindent\textit{End of Answer Key}
 
 \end{document}
 
