\documentclass[12pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{textcomp}
\usepackage{enumitem}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{amsmath, amssymb}
\geometry{margin=1in}
\begin{document}
\begin{center}
Machine Learning - Graduate Level Assessment 14 \
Total Marks: 40 \
Answer all questions.
\end{center}

\section*{Multiple Choice (10 marks)}
\begin{enumerate}[label=\arabic*.]
\item Adding more basis functions in a linear model, pick the most probably option:
\begin{enumerate}[label=(\alph*)]
    \item Decreases model bias
    \item Decreases estimation bias
    \item Decreases variance
    \item Doesn't affect bias and variance
\end{enumerate}
\item Statement 1| Layer Normalization is used in the original ResNet paper, not Batch Normalization. Statement 2| DCGANs use self-attention to stabilize training.
\begin{enumerate}[label=(\alph*)]
    \item True, True
    \item False, False
    \item True, False
    \item False, True
\end{enumerate}
\item If your training loss increases with number of epochs, which of the following could be a possible issue with the learning process?
\begin{enumerate}[label=(\alph*)]
    \item Regularization is too low and model is overfitting
    \item Regularization is too high and model is underfitting
    \item Step size is too large
    \item Step size is too small
\end{enumerate}
\item If N is the number of instances in the training dataset, nearest neighbors has a classification run time of
\begin{enumerate}[label=(\alph*)]
    \item O(1)
    \item O( N )
    \item O(log N )
    \item O( $N^2$ )
\end{enumerate}
\item What is the dimensionality of the null space of the following matrix? A = [[1, 1, 1], [1, 1, 1], [1, 1, 1]]
\begin{enumerate}[label=(\alph*)]
    \item 0
    \item 1
    \item 2
    \item 3
\end{enumerate}
\end{enumerate}

\section*{True / False (10 marks)}
\textit{Answer True or False}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{5}
\item True or False: Neural networks trained with gradient descent do not always converge to the global optimum, particularly in non-convex landscapes.
\item True or False: The computational complexity of gradient descent is polynomial in the number of features, denoted as D.
\item True or False: In Naive Bayes, attributes are statistically independent of one another given the class value.
\item True or False: Increasing the amount of training data will always eliminate overfitting in machine learning models.
\item True or False: ResNeXt models in 2018 predominantly utilized ReLU activation functions instead of tanh.
\end{enumerate}

\section*{Long Form Response (20 marks)}
\textit{Answer each question with a clear and complete explanation.}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{10}
\item Discuss the potential reasons for the poor performance of a decision tree in spam classification, particularly focusing on the implications of using shallow trees.
\item Discuss how to generate a $10 \times 5$ Gaussian matrix with entries sampled from $\mathcal{N}(\mu=5,\sigma^2=16)$ and a $10 \times 10$ uniform matrix from $U[-1,1)$. Provide the commands used in PyTorch and explain the rationale behind each step.
\end{enumerate}

\vfill
\noindent\textit{End of Paper}
\end{document}