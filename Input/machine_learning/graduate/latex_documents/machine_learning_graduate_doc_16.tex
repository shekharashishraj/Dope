\documentclass[12pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{textcomp}
\usepackage{enumitem}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{amsmath, amssymb}
\geometry{margin=1in}
\begin{document}
\begin{center}
Machine Learning - Graduate Level Assessment 16 \
Total Marks: 40 \
Answer all questions.
\end{center}

\section*{Multiple Choice (10 marks)}
\begin{enumerate}[label=\arabic*.]
\item Which of the following are the spatial clustering algorithms?
\begin{enumerate}[label=(\alph*)]
    \item Partitioning based clustering
    \item K-means clustering
    \item Grid based clustering
    \item All of the above
\end{enumerate}
\item Another term for out-of-distribution detection is?
\begin{enumerate}[label=(\alph*)]
    \item anomaly detection
    \item one-class detection
    \item train-test mismatch robustness
    \item background detection
\end{enumerate}
\item Which of the following points would Bayesians and frequentists disagree on?
\begin{enumerate}[label=(\alph*)]
    \item The use of a non-Gaussian noise model in probabilistic regression.
    \item The use of probabilistic modelling for regression.
    \item The use of prior distributions on the parameters in a probabilistic model.
    \item The use of class priors in Gaussian Discriminant Analysis.
\end{enumerate}
\item Which of the following best describes the joint probability distribution P(X, Y, Z) for the given Bayes net. X \textless{}- Y -\textgreater{} Z?
\begin{enumerate}[label=(\alph*)]
    \item P(X, Y, Z) = P(Y) * P(X|Y) * P(Z|Y)
    \item P(X, Y, Z) = P(X) * P(Y|X) * P(Z|Y)
    \item P(X, Y, Z) = P(Z) * P(X|Z) * P(Y|Z)
    \item P(X, Y, Z) = P(X) * P(Y) * P(Z)
\end{enumerate}
\item Which of the following best describes what discriminative approaches try to model? (w are the parameters in the model)
\begin{enumerate}[label=(\alph*)]
    \item p(y|x, w)
    \item p(y, x)
    \item p(w|x, w)
    \item None of the above
\end{enumerate}
\end{enumerate}

\section*{True / False (10 marks)}
\textit{Answer True or False}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{5}
\item True or False: Clustering is effectively used for detecting fraudulent credit card transactions by grouping similar transaction patterns.
\item True or False: The rank of the matrix A = [[1, 1, 1], [1, 1, 1], [1, 1, 1]] is 1 due to its linearly dependent rows.
\item True or False: The Penn Treebank is primarily focused on sentiment analysis and does not include language modeling applications.
\item True or False: The dimensionality of the null space of the matrix A is 2.
\item True or False: In the given Bayesian network, 15 independent parameters are required when no assumptions about independence are made.
\end{enumerate}

\section*{Long Form Response (20 marks)}
\textit{Answer each question with a clear and complete explanation.}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{10}
\item Write a function to extract maximum and minimum k elements in the given tuple.
\item Write a function to sort a given list of elements in ascending order using heap queue algorithm.
\end{enumerate}

\vfill
\noindent\textit{End of Paper}
\end{document}