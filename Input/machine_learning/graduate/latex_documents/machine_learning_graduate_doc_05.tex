\documentclass[12pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{textcomp}
\usepackage{enumitem}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{amsmath, amssymb}
\geometry{margin=1in}
\begin{document}
\begin{center}
Machine Learning - Graduate Level Assessment 5 \
Total Marks: 40 \
Answer all questions.
\end{center}

\section*{Multiple Choice (10 marks)}
\begin{enumerate}[label=\arabic*.]
\item Suppose your model is overfitting. Which of the following is NOT a valid way to try and reduce the overfitting?
\begin{enumerate}[label=(\alph*)]
    \item Increase the amount of training data.
    \item Improve the optimisation algorithm being used for error minimisation.
    \item Decrease the model complexity.
    \item Reduce the noise in the training data.
\end{enumerate}
\item The disadvantage of Grid search is
\begin{enumerate}[label=(\alph*)]
    \item It can not be applied to non-differentiable functions.
    \item It can not be applied to non-continuous functions.
    \item It is hard to implement.
    \item It runs reasonably slow for multiple linear regression.
\end{enumerate}
\item Which of the following sentence is FALSE regarding regression?
\begin{enumerate}[label=(\alph*)]
    \item It relates inputs to outputs.
    \item It is used for prediction.
    \item It may be used for interpretation.
    \item It discovers causal relationships
\end{enumerate}
\item Statement 1| As of 2020, some models attain greater than 98\% accuracy on CIFAR-10. Statement 2| The original ResNets were not optimized with the Adam optimizer.
\begin{enumerate}[label=(\alph*)]
    \item True, True
    \item False, False
    \item True, False
    \item False, True
\end{enumerate}
\item Which among the following prevents overfitting when we perform bagging?
\begin{enumerate}[label=(\alph*)]
    \item The use of sampling with replacement as the sampling technique
    \item The use of weak classifiers
    \item The use of classification algorithms which are not prone to overfitting
    \item The practice of validation performed on every classifier trained
\end{enumerate}
\end{enumerate}

\section*{True / False (10 marks)}
\textit{Answer True or False}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{5}
\item True or False: Pruning a Decision Tree is primarily performed to avoid overfitting the training set, enhancing generalization to new data.
\item True or False: The convergence of a neural network is significantly influenced by the choice of learning rate during training.
\item True or False: The Bayesian network H -\textgreater{} U \textless{}- P \textless{}- W requires 8 independent parameters for its complete specification.
\item True or False: Bayesians advocate for the use of prior distributions on parameters in probabilistic models, whereas frequentists do not.
\item True or False: In Naive Bayes, attributes are statistically independent of one another given the class value.
\end{enumerate}

\section*{Long Form Response (20 marks)}
\textit{Answer each question with a clear and complete explanation.}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{10}
\item Write a function to calculate the height of the given binary tree.
\item Write a function to count the elements in a list until an element is a tuple.
\end{enumerate}

\vfill
\noindent\textit{End of Paper}
\end{document}