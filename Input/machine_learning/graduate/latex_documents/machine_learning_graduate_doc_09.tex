\documentclass[12pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{textcomp}
\usepackage{enumitem}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{amsmath, amssymb}
\geometry{margin=1in}
\begin{document}
\begin{center}
Machine Learning - Graduate Level Assessment 9 \
Total Marks: 40 \
Answer all questions.
\end{center}

\section*{Multiple Choice (10 marks)}
\begin{enumerate}[label=\arabic*.]
\item Which of the following sentence is FALSE regarding regression?
\begin{enumerate}[label=(\alph*)]
    \item It relates inputs to outputs.
    \item It is used for prediction.
    \item It may be used for interpretation.
    \item It discovers causal relationships
\end{enumerate}
\item Which among the following prevents overfitting when we perform bagging?
\begin{enumerate}[label=(\alph*)]
    \item The use of sampling with replacement as the sampling technique
    \item The use of weak classifiers
    \item The use of classification algorithms which are not prone to overfitting
    \item The practice of validation performed on every classifier trained
\end{enumerate}
\item Another term for out-of-distribution detection is?
\begin{enumerate}[label=(\alph*)]
    \item anomaly detection
    \item one-class detection
    \item train-test mismatch robustness
    \item background detection
\end{enumerate}
\item Suppose your model is overfitting. Which of the following is NOT a valid way to try and reduce the overfitting?
\begin{enumerate}[label=(\alph*)]
    \item Increase the amount of training data.
    \item Improve the optimisation algorithm being used for error minimisation.
    \item Decrease the model complexity.
    \item Reduce the noise in the training data.
\end{enumerate}
\item If N is the number of instances in the training dataset, nearest neighbors has a classification run time of
\begin{enumerate}[label=(\alph*)]
    \item O(1)
    \item O( N )
    \item O(log N )
    \item O( $N^2$ )
\end{enumerate}
\end{enumerate}

\section*{True / False (10 marks)}
\textit{Answer True or False}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{5}
\item True or False: Linear hard-margin SVM can only be applied to datasets that are linearly separable without any misclassification.
\item True or False: Bayesians advocate for the use of prior distributions on parameters in a probabilistic model, unlike frequentists.
\item True or False: RoBERTa pretrains on a corpus that is approximately 10 times larger than the corpus used for BERT's pretraining.
\item True or False: The rank of the matrix A = [[1, 1, 1], [1, 1, 1], [1, 1, 1]] is 1, indicating a single linearly independent row.
\item True or False: Neural networks can utilize a mix of different activation functions within their architecture.
\end{enumerate}

\section*{Long Form Response (20 marks)}
\textit{Answer each question with a clear and complete explanation.}
\begin{enumerate}[label=\arabic*.]
\setcounter{enumi}{10}
\item Write a function to find the index of the last occurrence of a given number in a sorted array.
\item Write a python function to find the maximum length of sublist.
\end{enumerate}

\vfill
\noindent\textit{End of Paper}
\end{document}